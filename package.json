{
  "name": "obsidian-beauty-diagram",
  "version": "0.1.13",
  "description": "Render Mermaid and PlantUML in Obsidian via the Beauty Diagram API.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "preversion": "npm run typecheck && npm test && npm run build",
    "version": "node version-bump.mjs && node scripts/update-changelog.mjs && git add manifest.json versions.json CHANGELOG.md",
    "postversion": "git push --follow-tags origin main"
  },
  "keywords": [
    "obsidian-plugin",
    "mermaid",
    "plantuml",
    "diagram"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "fake-indexeddb": "^5.0.2",
    "jsdom": "^24.0.0",
    "obsidian": "latest",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
